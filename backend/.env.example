# Local development — for production substitute docker Compose service names
# Example: postgres service host `postgres`, redis `redis`, chromadb `chromadb`.

# ===== LLM ROUTING (LiteLLM) =====
# Optional executor hint mirrors dashboard copy (primary chain still flows through WORKFLOW_BREAKER_*).
# LLM_MODEL=xai/grok-3-mini
GROK_API_KEY=xai-your-grok-key-here
ANTHROPIC_API_KEY=sk-ant-your-claude-key-here
OPENAI_API_KEY=sk-your-openai-key-optional

# ===== AUTO WORKFLOW BREAKER (LiteLLM) =====
WORKFLOW_BREAKER_PRIMARY_MODEL=xai/grok-3-mini
WORKFLOW_BREAKER_FALLBACK_MODEL=anthropic/claude-haiku-4-5-20251001
WORKFLOW_BREAKER_TERTIARY_MODEL=openai/gpt-4o-mini
# Ak Grok ani Claude nepôjdu, tertiary sa použije len pri nastavenom OPENAI_API_KEY.
WORKFLOW_BREAKER_MAX_OUTPUT_TOKENS=4096
WORKFLOW_BREAKER_TEMPERATURE=0.15

# ===== DATABASES (localhost) =====
POSTGRES_URL=postgresql+asyncpg://queenswarm:devpass@localhost:5432/queenswarm
POSTGRES_USER=queenswarm
POSTGRES_PASSWORD=devpass
POSTGRES_DB=queenswarm

REDIS_URL=redis://localhost:6379/0
# Optional: separate broker/results for Celery (default: REDIS_URL).
# CELERY_BROKER_URL=redis://localhost:6379/1
# CELERY_RESULT_BACKEND=redis://localhost:6379/2
# Horizontal scaling mode (enterprise / multi-instance):
# SCALING_MODE_ENABLED=false
# INSTANCE_ID=api-node-a
# WORKER_COUNT=1
# DISTRIBUTED_LOCK_TTL_SEC=45
# DISTRIBUTED_LOCK_RENEW_INTERVAL_SEC=15.0
# HA_MODE_ENABLED=false
# REDIS_FAILOVER_URLS=redis://redis-replica:6379/0
# POSTGRES_REPLICA_URLS=
# GRACEFUL_SHUTDOWN_TIMEOUT_SEC=15
# GRACEFUL_SHUTDOWN_DRAIN_SEC=4

NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme_strong_password

CHROMA_HOST=localhost
CHROMA_PORT=8000
VECTOR_STORE_BACKEND=pgvector

# ===== BALLROOM =====
# Capsule JSON: redis (default, multi-worker) or memory (single-process / local tests).
# BALLROOM_CAPSULE_BACKEND=redis
# BALLROOM_CAPSULE_TTL_SEC=86400
# VOICE_ENABLED=false
# VOICE_STT_MODEL=whisper-1
# Optional higher-fidelity TTS provider:
# ELEVENLABS_API_KEY=
# ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
# VOICE_TTS_MODEL=eleven_multilingual_v2
# Fallback TTS via OpenAI when ElevenLabs key is missing:
# VOICE_TTS_MODEL_OPENAI=gpt-4o-mini-tts
# VOICE_TTS_OPENAI_VOICE=alloy
# xAI Grok TTS (when XAI_API_KEY is set):
# VOICE_TTS_XAI_VOICE_ID=Ara
# VOICE_TTS_XAI_LANGUAGE=en
# VOICE_TTS_XAI_OPTIMIZE_STREAMING_LATENCY=0
# VOICE_TTS_XAI_OUTPUT_CODEC=
# VOICE_TTS_XAI_SAMPLE_RATE=24000
# VOICE_TTS_XAI_BIT_RATE=128000

# ===== BEE-HIVE TUNING =====
SUB_SWARM_SIZE=8
HIVE_SYNC_INTERVAL_SEC=300
REWARD_THRESHOLD_PASS=0.7
# Pollen per bee after a fully verified swarm LangGraph run (0 disables):
# VERIFIED_SWARM_POLLEN_PER_BEE=1.0
RAPID_LOOP_TIMEOUT_SEC=60
IMITATION_TOP_K=3
MEMORY_DECAY_DAYS=14
SCOUT_SWARM_SIZE=8
EVAL_SWARM_SIZE=6
SIM_SWARM_SIZE=5
ACTION_SWARM_SIZE=10
HIVE_WAGGLE_RELAY_ENABLED=true
# Outcome gate: simulator must emit verification_passed + confidence ≥ REWARD_THRESHOLD_PASS.
# EXPOSE_RAW_STEP_OUTPUTS=false
# Simulator GenericBee emits synthetic verification (testing only — keep false in prod).
# SIMULATOR_STUB_AUTO_VERIFY=false
# Disable POST …/run? defer_to_worker if you omit workers:
# CELERY_WORKFLOW_RUNS_ENABLED=true
# Persist simulations rows after each swarm LangGraph cycle (synthetic until Docker binds):
# SIMULATION_AUDIT_ROWS_ENABLED=true
# SIMULATION_DOCKER_EXECUTION_ENABLED=false
# Ephemeral probe after each cycle when Docker mode is on (requires docker CLI on host):
# SIMULATION_DOCKER_IMAGE=busybox:1.36
# SIMULATION_DOCKER_TIMEOUT_SEC=30
# SIMULATION_DOCKER_MEMORY_MB=256
# SIMULATION_DOCKER_CPU_LIMIT=0.5
# SIMULATION_DOCKER_LOG_TRUNCATE_CHARS=8192
# Hard runtime caps for simulation/LLM pressure:
# SIMULATION_MAX_PARALLEL=2
# LLM_MAX_CONCURRENCY=6
# CELERY_WORKER_CONCURRENCY=3
# LLM_RETRY_MAX_ATTEMPTS=3
# LLM_RETRY_INITIAL_WAIT_SEC=0.35
# LLM_RETRY_MAX_WAIT_SEC=4.0
# Recipe semantic search (GET /api/v1/recipes/search):
# RECIPE_CHROMA_SEARCH_LIMIT_CAP=20
# RECIPE_CHROMA_MIN_SIMILARITY=0.0
# Breaker Chrom hint cosine cutoff (Recipe Library retrieval):
# RECIPE_LIBRARY_MATCH_THRESHOLD=0.85
# After verified swarm cycles, upsert matching_recipe into Chroma (needs reachable Chroma):
# RECIPE_CHROMA_AUTO_SYNC_ON_VERIFY=false
# POST/PATCH /api/v1/recipes mirror Chroma embeddings (disable if cluster unavailable):
# RECIPE_WRITE_SYNC_CHROMA=true
# Recipe catalog POST/PATCH default off in production — enable with trusted principals only:
# RECIPE_CATALOG_MUTATIONS_ENABLED=false
# Optional JWT sub allowlist (CSV); when set, only these subjects may POST/PATCH/DELETE recipes:
# RECIPE_CATALOG_MUTATION_ALLOWLIST=hive-ci,recipe-keeper
# Optional OAuth scope string that must appear in the JWT ``scope`` claim (from POST /auth/token):
# RECIPE_CATALOG_MUTATION_REQUIRED_SCOPE=recipes:write
# Max JSON-encoded workflow_template size (bytes):
# RECIPE_WORKFLOW_TEMPLATE_MAX_JSON_BYTES=262144

# ===== SECURITY =====
SECRET_KEY=dev-secret-change-me-to-at-least-thirty-two-characters
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
# Enable Settings → 2FA management and scripted API keys (403 on /auth/profile/totp/* and /auth/api-keys when false).
ENABLE_2FA=true
SECURITY_2FA_ADVANCED_ENABLED=true
API_KEY_MANAGEMENT_ENABLED=true
# Strict production hardening mode (recommended true in prod, false in local/staging bring-up).
PRODUCTION_SECURITY_MODE=false
# Optional M2M mint: POST /api/v1/auth/token (HTTP Basic; secret ≥32 chars). Set both or neither.
# HIVE_TOKEN_CLIENT_ID=hive-ci
# HIVE_TOKEN_CLIENT_SECRET=change-me-to-at-least-thirty-two-characters!!
# Keep false for security: issued JWT sub is pinned to HIVE_TOKEN_CLIENT_ID.
# HIVE_TOKEN_ALLOW_SUBJECT_OVERRIDE=false
# User-uploaded Python plugins are dangerous; leave disabled unless explicitly needed.
# ALLOW_USER_PLUGIN_UPLOADS=false
# Optional explicit Fernet key for connector/OAuth/API-key vault payloads.
# Required when PRODUCTION_SECURITY_MODE=true.
# CONNECTOR_VAULT_FERNET_KEY=
# Enable interactive 2FA login challenge feature flag.
# ENABLE_2FA=false
RATE_LIMIT_ENABLED=true
RATE_LIMIT_TRUST_FORWARDED_HEADERS=true
TRUSTED_PROXY_HOPS=1
RATE_LIMIT_BURST_MAX=10
RATE_LIMIT_BURST_WINDOW_SEC=1.0
RATE_LIMIT_SUSTAIN_MAX=100
RATE_LIMIT_SUSTAIN_WINDOW_SEC=60.0
RATE_LIMIT_LOGIN_MAX=20
RATE_LIMIT_LOGIN_WINDOW_SEC=300.0
RATE_LIMIT_LOGIN_IDENTITY_MAX=8
RATE_LIMIT_LOGIN_IDENTITY_WINDOW_SEC=300.0
RATE_LIMIT_TOKEN_EXCHANGE_MAX=30
RATE_LIMIT_TOKEN_EXCHANGE_WINDOW_SEC=300.0
# Optional authenticated user throttles (recommended true in production).
# RATE_LIMIT_USER_ENABLED=false
# RATE_LIMIT_USER_SUSTAIN_MAX=240
# RATE_LIMIT_USER_SUSTAIN_WINDOW_SEC=60.0
# RATE_LIMIT_USER_ENDPOINT_MAX=90
# RATE_LIMIT_USER_ENDPOINT_WINDOW_SEC=60.0
HEALTH_READINESS_CACHE_SEC=3.0
# HEALTH_DEPENDENCY_TIMEOUT_SEC=2.0
# When true, /health/ready returns 503 if Neo4j or Chroma checks fail:
# READINESS_REQUIRE_NEO4J=false
# READINESS_REQUIRE_CHROMA=false

# ===== DOMAIN & CORS =====
DOMAIN=queenswarm.love
CORS_ORIGINS=https://queenswarm.love,https://www.queenswarm.love,http://localhost:3000

# ===== PHASE 6.0 — SUPERVISOR SESSIONS =====
# Dynamic supervisor sessions and sub-agent orchestration APIs:
# SUPERVISOR_DYNAMIC_SUBAGENTS_ENABLED=false
# SUPERVISOR_DURABLE_MODE_ENABLED=false
# SUPERVISOR_DEFAULT_RUNTIME_MODE=inprocess
# SUPERVISOR_EVENT_LOG_LIMIT=500
# SUPERVISOR_SKILLS_ENABLED=false
# SUPERVISOR_MAX_SKILLS_PER_AGENT=5
# SUPERVISOR_SELF_HEALING_ENABLED=true
# SUPERVISOR_SELF_HEAL_MAX_ATTEMPTS=2
# SUPERVISOR_AUTONOMY_ENABLED=true
# RETRIEVAL_CONTRACT_ENABLED=false
# RETRIEVAL_V2_MIN_RELEVANCE_SCORE=0.22
# RETRIEVAL_V2_MAX_ITEMS_PER_SECTION=6
# LIGHT_CONTROL_PLANE_ENABLED=false
# OPERATOR_CONTROL_PLANE_ENABLED=true
# OPERATOR_ICM_TOOLS_ENABLED=true   # Link Drop, Dialogue Extract, keyword hints (on-demand)
# HIVE_INNOVATION_LAB_ENABLED=true
# GROK_CONTROL_PLANE_ENABLED=true
# GROK_CP_CLI_ENABLED=true
# GROK_CP_EXECUTE_COMMANDS=false
# GROK_CP_ALLOW_PROD_DEPLOY=false
# GROK_CP_REQUIRE_APPROVAL_FOR_RISK=medium,high,critical
# GROK_CP_DEDUP_HARD_GATE_ENABLED=true
# GROK_CP_DEDUP_REUSE_THRESHOLD=0.62
# GROK_CP_DEDUP_HYBRID_THRESHOLD=0.35
# GROK_CP_DEDUP_MIN_SCORE_BY_SOURCE=task:0.12,recipe:0.18,knowledge:0.16,grok_run:0.20
# GROK_CP_DEDUP_WEIGHT_BY_SOURCE=task:1.0,recipe:1.1,knowledge:0.95,grok_run:1.15
# GROK_CP_HIVEMIND_REVIEW_ALERT_THRESHOLD=10
# GROK_CP_HIVEMIND_REVIEW_ALERT_COOLDOWN_SEC=900
# GROK_CP_HIVEMIND_REVIEW_ALERT_MAX_AGE_HOURS=24
# GROK_CP_COST_CAP_USD_24H=25
# GROK_CP_ESTIMATED_COST_PER_RUN=read_only:0.03,code_edit:0.12,code_edit_and_test:0.30,deploy_candidate:0.55,prod_deploy:0.80
# GROK_CP_TIMEOUT_ALERT_THRESHOLD_24H=3
# GROK_CP_RISK_ESCALATION_THRESHOLD_24H=6
# GROK_CP_ESCALATION_DEDUP_ENABLED=true
# GROK_CP_ESCALATION_COOLDOWN_SEC=600
# GROK_CP_LAST_RESUMED_MARKER_TTL_HOURS=24
# OPERATOR_TELEGRAM_INBOUND_ENABLED=true
# PROOF_OF_HIVE_ENABLED=true
# HIVE_ORACLE_ENABLED=true
# INTENT_CRYSTALLIZER_ENABLED=true
# NEXT_PUBLIC_OPERATOR_CONTROL_PLANE_ENABLED=true
# ROUTINES_ENABLED=false
# ROUTINE_WATCH_INTERVAL_SEC=120
# ROUTINE_HISTORY_MAX_ENTRIES=50
# MEMORY_EVOLUTION_ENABLED=true
# MEMORY_EVOLUTION_INTERVAL_SEC=900
# MEMORY_EVOLUTION_MANUAL_APPROVAL_THRESHOLD=0.82
# AGENT_INITIATIVE_ENABLED=true
# AGENT_INITIATIVE_AUTO_APPROVE_ENABLED=true
# AGENT_INITIATIVE_AUTO_APPROVE_MAX_RISK_SCORE=0.25
# AGENT_INITIATIVE_AUTO_APPROVE_MAX_IMPACT_SCORE=0.55
# SWARM_FULL_AUTONOMY_ENABLED=true
# AUTONOMOUS_ROUTINES_ENABLED=true
# AUTONOMOUS_ROUTINE_PLANNING_HORIZON_HOURS=72
# BROWSER_HARNESS_ENABLED=true
# BROWSER_ACTION_TIMEOUT_SEC=20
# BROWSER_SESSION_TTL_SEC=240
# BROWSER_MAX_ACTIONS_PER_SESSION=24
# BROWSER_MAX_CONCURRENT_SESSIONS=6
# BROWSER_INSTANCE_CPU_LIMIT=0.5
# BROWSER_INSTANCE_MEMORY_MB=256
# BROWSER_ALLOWED_DOMAINS=["example.com","www.example.com","queenswarm.love"]

# ===== PHASE 7.0 — CONSOLIDATION / ADVANCED MODULE FLAGS =====
# Conservative defaults: core ON, advanced modules OFF until explicitly enabled.
# ADVANCED_MONITORING_ENABLED=false
# SIMULATIONS_ENABLED=true
# LEADERBOARD_ENABLED=true
# VERIFIED_POLLEN_LEADERBOARD_ENABLED=true
# RECIPES_ENABLED=true
# SKILL_EXPORT_PREMIUM_ENABLED=true
# PAPER_TRADING_ENABLED=true
# PAPER_TRADING_TICK_INTERVAL_SEC=900
# SECURITY_2FA_ADVANCED_ENABLED=true
# API_KEY_MANAGEMENT_ENABLED=true
# PHASE70_CONSOLIDATED_NAV_ENABLED=true
# OAUTH_CALLBACK_RATE_PER_IP=30
# OAUTH_CALLBACK_RATE_WINDOW_SEC=60.0
# DYNAMIC_CONNECTOR_REGISTRY_CACHE_TTL_SEC=300
# DYNAMIC_CONNECTOR_RATE_LIMIT_PER_MINUTE=120
# DYNAMIC_CONNECTOR_TOOL_RATE_LIMIT_PER_MINUTE=60
# DYNAMIC_CONNECTOR_CIRCUIT_FAILURE_THRESHOLD=3
# DYNAMIC_CONNECTOR_CIRCUIT_OPEN_SEC=90.0
# DYNAMIC_CONNECTOR_TOOL_TIMEOUT_MS=2500
# GROKIPEDIA_BASE_URL=
# PHASE3_OBSIDIAN_WATCH_ENABLED=false
# PHASE3_OBSIDIAN_POLL_INTERVAL_SEC=300
# PHASE3_OBSIDIAN_MAX_FILES_PER_SYNC=50
# ENTERPRISE_MONITORING_ENABLED=false
# OPENTELEMETRY_ENABLED=false
# OPENTELEMETRY_SERVICE_NAME=queenswarm-api
# OPENTELEMETRY_EXPORTER_OTLP_ENDPOINT=
# LangFuse tracing for LiteLLM hops (success/failure callbacks):
# LANGFUSE_ENABLED=false
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_HOST=https://cloud.langfuse.com

# ===== OUTCOME GATE (pending review) =====
# PENDING_REVIEW_ENABLED=true
# PENDING_REVIEW_CONFIDENCE_THRESHOLD=0.75
# PENDING_REVIEW_NOTIFY_SLACK=true

# ===== READINESS / AGENT RECOVERY =====
# READINESS_REQUIRE_CELERY=false
# AGENT_STALE_SWEEP_ENABLED=true
# AGENT_STALE_TIMEOUT_SEC=600

# ===== RECIPE HYBRID SCORING =====
# RECIPE_HYBRID_SCORING_ENABLED=true
# RECIPE_HYBRID_VECTOR_WEIGHT=0.85
# RECIPE_HYBRID_GRAPH_WEIGHT=0.15
# RECIPE_CHROMA_MIN_SIMILARITY=0.5
# RECIPE_CHROMA_AUTO_SYNC_ON_VERIFY=true
# RECIPE_HYBRID_NEO4J_ENABLED=true

# ===== ENTERPRISE OBSERVABILITY / ALERTING =====
# LOG_LEVEL=INFO
# LOG_ENVIRONMENT=development
# LOG_SERVICE_NAME=queenswarm-api
# ALERTING_ENABLED=false
# ALERT_MEMORY_PERCENT_THRESHOLD=88.0
# ALERT_SUPERVISOR_FAILURES_THRESHOLD=1
# ALERT_RATE_LIMIT_BLOCKS_5M_THRESHOLD=40
# ALERT_SCALING_EVENTS_5M_THRESHOLD=5
# ALERT_DISPATCH_COOLDOWN_SEC=300
# PAGERDUTY_ROUTING_KEY=
# PAGERDUTY_EVENTS_API_URL=https://events.pagerduty.com/v2/enqueue

# ===== EMAIL (agents → deliver_to_email) =====
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=your-app-password
NOTIFY_EMAIL=operator@your-domain.com
# Optional Slack webhook legacy (majority of operators use SMTP only).
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
# LSP_MCP_BRIDGE_ENABLED=false            # Symbol index → MCP tools for coder sub-agents
# RUBRIC_TEMPLATES_ENABLED=true           # Subjective scoring rubrics in harness + workflows
# FORAGER_INTELLIGENCE_LOOP_ENABLED=false # Daily Celery scan for skill/MCP/doc proposals
# FORAGER_INTELLIGENCE_CRON_HOUR=6        # UTC hour for daily intelligence tick
# SELF_EXTENDING_TOOL_MARKETPLACE_ENABLED=true # Forager scan → one-click MCP preset install
# QUEEN_MAINTAINER_ENABLED=false          # Weekly Maintainer routine + HTTP controls
# QUEEN_MAINTAINER_SESSION_CAP_USD=0.50     # Per Maintainer session LLM cap
# QUEEN_MAINTAINER_DAILY_RUN_LIMIT=1        # Max Maintainer runs per tenant per UTC day
# QUEEN_MAINTAINER_ROUTING_MODE=economy     # economy | free_first (cheap models first)
# QUEEN_MAINTAINER_CODER_MODEL=xai/grok-3-mini
# QUEEN_MAINTAINER_CRITIC_MODEL=openai/gpt-4o-mini
# QUEEN_MAINTAINER_SELF_HEAL_MAX_ATTEMPTS=1
# QUEEN_MAINTAINER_POST_MERGE_WEBHOOK_ENABLED=false
# QUEEN_MAINTAINER_GITHUB_WEBHOOK_SECRET=   # GitHub webhook HMAC secret
# QUEEN_MAINTAINER_POST_MERGE_TENANT_ID=    # Tenant UUID for post-merge triggers
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# SUPERVISOR_AUDIT_DIGEST_ENABLED=false
# SUPERVISOR_AUDIT_DIGEST_WINDOW_HOURS=24
# SUPERVISOR_AUDIT_DIGEST_SLACK_ENABLED=true
# SUPERVISOR_AUDIT_DIGEST_DISCORD_ENABLED=true
# TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/...
# SUPERVISOR_AUDIT_DIGEST_TEAMS_ENABLED=true
# SUPERVISOR_AUDIT_ROLLUP_EMAIL_ENABLED=false
# SUPERVISOR_AUDIT_ROLLUP_WINDOW_HOURS=168
# SUPERVISOR_AUDIT_ROLLUP_CACHE_TTL_SEC=300
NOTION_API_KEY=secret_your_notion_integration_key

# ===== SCRAPING =====
YOUTUBE_API_KEY=your-youtube-data-api-v3-key
USER_AGENT=QueenswarmBot/2.0
PROXY_LIST_URL=

# ===== COST GOVERNOR =====
DAILY_BUDGET_USD=10.00
WEEKLY_BUDGET_USD=50.00
MONTHLY_BUDGET_USD=200.00
COST_WARNING_THRESHOLD=0.8

# Loaded automatically by `next dev`. Docker Compose still overrides via container env.
INTERNAL_BACKEND_ORIGIN=http://127.0.0.1:8000
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000/api/v1
NEXT_PUBLIC_OPERATOR_CONTROL_PLANE_ENABLED=true
# Opt-in only: shows "Skip 2FA (dev)" on login (never set in production builds).
# NEXT_PUBLIC_ALLOW_DEV_LOGIN_BYPASS=true
# NEXT_PUBLIC_TERMS_URL=https://queenswarm.love/terms
# NEXT_PUBLIC_PRIVACY_URL=https://queenswarm.love/privacy

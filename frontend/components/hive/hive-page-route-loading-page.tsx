import { HivePageShellSkeleton } from "@/components/hive/hive-page-shell-skeleton";

/** Default App Router loading segment — HivePageShell-shaped skeleton. */
export default function HivePageRouteLoadingPage() {
  return <HivePageShellSkeleton />;
}

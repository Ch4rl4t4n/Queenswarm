"use client";

import { memo } from "react";

/** Horizontal in-app marketplace deprioritized — harness packs sell on Gumroad/GitHub. */
function RecipeMarketplaceBetaPanelInner(): null {
  return null;
}

export const RecipeMarketplaceBetaPanel = memo(RecipeMarketplaceBetaPanelInner);
RecipeMarketplaceBetaPanel.displayName = "RecipeMarketplaceBetaPanel";

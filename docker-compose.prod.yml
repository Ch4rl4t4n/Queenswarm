# Production overlay — https://queenswarm.love
# Usage: ./scripts/deploy-prod.sh
#
# Phase 5.2: **~32 GB RAM** single-host posture (raise ``docker-compose.base.yml`` ceilings for DB, API, workers).
# For ~16 GB hosts, fork this file or override ``mem_limit`` via a local gitignored compose snippet.
#
# docker compose -p queenswarm_prod -f docker-compose.base.yml -f docker-compose.prod.yml --env-file .env.prod up -d

name: queenswarm_prod

# Production: never publish data-plane ports to the public host — only nginx 80/443.
services:
  postgres:
    mem_limit: 5120m
    cpus: "2.0"
    ports: !reset []

  redis:
    mem_limit: 1024m
    cpus: "0.75"
    ports: !reset []
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?set REDIS_PASSWORD in .env.prod — run scripts/ensure-redis-password.sh}
    command:
      - redis-server
      - --save
      - "60"
      - "1"
      - --loglevel
      - warning
      - --requirepass
      - ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep -q PONG"]
      interval: 10s
      timeout: 3s
      retries: 12

  neo4j:
    environment:
      NEO4J_server_memory_heap_initial__size: 1G
      NEO4J_server_memory_heap_max__size: 3G
    mem_limit: 4096m
    cpus: "1.5"
    ports: !reset []

  backend:
    env_file:
      - ${QS_ENV_FILE_PROD:-.env.prod}
      - path: ${QS_ENV_FILE_PROD_TOKENS:-.env.prod.tokens}
        required: false
      - path: ${QS_ENV_FILE_PROD_OAUTH:-.env.prod.oauth}
        required: false
    environment:
      API_KEY_MANAGEMENT_ENABLED: ${API_KEY_MANAGEMENT_ENABLED:-true}
      SOLO_MODE_ENABLED: ${SOLO_MODE_ENABLED:-false}
      DR_REPORTS_DIR: ${DR_REPORTS_DIR:-/app/reports/dr}
      HA_REPORTS_DIR: ${HA_REPORTS_DIR:-/app/reports/ha}
      LLM_MAX_CONCURRENCY: ${LLM_MAX_CONCURRENCY:-8}
      SIMULATION_MAX_PARALLEL: ${SIMULATION_MAX_PARALLEL:-3}
      CELERY_WORKER_CONCURRENCY: ${CELERY_WORKER_CONCURRENCY:-3}
      CODEBASE_ATLAS_REPO_ROOT: /repo
    volumes:
      - ./reports:/app/reports:ro
      - ./exports:/app/exports
      - ./exports:/exports
      - .:/repo:ro
    mem_limit: 10240m
    cpus: "4.0"
    ports: !reset []

  celery-worker:
    env_file:
      - ${QS_ENV_FILE_PROD:-.env.prod}
      - path: ${QS_ENV_FILE_PROD_TOKENS:-.env.prod.tokens}
        required: false
      - path: ${QS_ENV_FILE_PROD_OAUTH:-.env.prod.oauth}
        required: false
    environment:
      LLM_MAX_CONCURRENCY: ${LLM_MAX_CONCURRENCY:-8}
      SIMULATION_MAX_PARALLEL: ${SIMULATION_MAX_PARALLEL:-3}
      CELERY_WORKER_CONCURRENCY: ${CELERY_WORKER_CONCURRENCY:-3}
    mem_limit: 3072m
    cpus: "1.75"
    volumes:
      - ./exports:/app/exports
      - ./exports:/exports

  celery-beat:
    env_file:
      - ${QS_ENV_FILE_PROD:-.env.prod}
      - path: ${QS_ENV_FILE_PROD_TOKENS:-.env.prod.tokens}
        required: false
      - path: ${QS_ENV_FILE_PROD_OAUTH:-.env.prod.oauth}
        required: false
    environment:
      LLM_MAX_CONCURRENCY: ${LLM_MAX_CONCURRENCY:-8}
      SIMULATION_MAX_PARALLEL: ${SIMULATION_MAX_PARALLEL:-3}
      CELERY_WORKER_CONCURRENCY: ${CELERY_WORKER_CONCURRENCY:-3}

  grafana:
    environment:
      GF_SERVER_DOMAIN: ${DOMAIN:-queenswarm.love}
      GF_SERVER_ROOT_URL: ${GF_SERVER_ROOT_URL:-https://queenswarm.love/grafana/}
    mem_limit: 768m
    cpus: "0.75"
    ports: !reset []

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE:-https://queenswarm.love/api/v1}
        NEXT_PUBLIC_HIVE_WS_GUEST: ${NEXT_PUBLIC_HIVE_WS_GUEST:-true}
        NEXT_PUBLIC_BALLROOM_GUEST_WS: ${NEXT_PUBLIC_BALLROOM_GUEST_WS:-true}
        NEXT_PUBLIC_QS_POLL_PROFILE: ${NEXT_PUBLIC_QS_POLL_PROFILE:-vps_32gb}
        NEXT_PUBLIC_SOLO_MODE: ${NEXT_PUBLIC_SOLO_MODE:-false}
        NEXT_PUBLIC_RECIPES_ENABLED: ${NEXT_PUBLIC_RECIPES_ENABLED:-true}
        NEXT_PUBLIC_VOICE_ENABLED: ${NEXT_PUBLIC_VOICE_ENABLED:-true}
        NEXT_PUBLIC_OPERATOR_CONTROL_PLANE_ENABLED: ${NEXT_PUBLIC_OPERATOR_CONTROL_PLANE_ENABLED:-true}
        NEXT_PUBLIC_PHASE70_CONSOLIDATED_NAV_ENABLED: ${NEXT_PUBLIC_PHASE70_CONSOLIDATED_NAV_ENABLED:-true}
    environment:
      NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE:-https://queenswarm.love/api/v1}
      NEXT_PUBLIC_HIVE_WS_GUEST: ${NEXT_PUBLIC_HIVE_WS_GUEST:-true}
      NEXT_PUBLIC_BALLROOM_GUEST_WS: ${NEXT_PUBLIC_BALLROOM_GUEST_WS:-true}
      NEXT_PUBLIC_QS_POLL_PROFILE: ${NEXT_PUBLIC_QS_POLL_PROFILE:-vps_32gb}
      NEXT_PUBLIC_SOLO_MODE: ${NEXT_PUBLIC_SOLO_MODE:-false}
      NEXT_PUBLIC_RECIPES_ENABLED: ${NEXT_PUBLIC_RECIPES_ENABLED:-true}
      NEXT_PUBLIC_VOICE_ENABLED: ${NEXT_PUBLIC_VOICE_ENABLED:-true}
      NEXT_PUBLIC_OPERATOR_CONTROL_PLANE_ENABLED: ${NEXT_PUBLIC_OPERATOR_CONTROL_PLANE_ENABLED:-true}
      NEXT_PUBLIC_PHASE70_CONSOLIDATED_NAV_ENABLED: ${NEXT_PUBLIC_PHASE70_CONSOLIDATED_NAV_ENABLED:-true}
      INTERNAL_BACKEND_ORIGIN: http://backend:8000
      HIVE_PROXY_JWT: ${DASHBOARD_JWT:-unset}
      NODE_ENV: production
    mem_limit: 1536m
    cpus: "1.25"
    ports: !reset []

  prometheus:
    mem_limit: 1024m
    cpus: "1.0"
    ports: !reset []

  nginx:
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/nginx/queenswarm.love.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/nginx/conf.d/letagentscook.org.conf:/etc/nginx/conf.d/letagentscook.org.conf:ro
      - ./deploy/nginx/.acme:/var/www/certbot:ro
      - ./deploy/nginx/includes:/etc/nginx/includes:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    mem_limit: 512m
    cpus: "0.75"

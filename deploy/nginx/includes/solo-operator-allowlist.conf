# Solo operator IP lock — managed by scripts/operator-solo-nginx-allowlist.sh
# Disabled: all rules commented (public HTTPS unchanged).
# Enable:  OPERATOR_IP=x.x.x.x ./scripts/operator-solo-nginx-allowlist.sh enable
# Disable: ./scripts/operator-solo-nginx-allowlist.sh disable

# allow 203.0.113.10/32;
# deny all;

# =============================================================================
# Queenswarm SOLO OPERATOR preset — merged into .env.prod by scripts/apply-solo-mode.sh
# =============================================================================
# One admin operator: full revenue stack (Stripe, marketplace, UGC, Factory, trading).
# Hides multi-tenant B2B only (team RBAC, enterprise workspace, accounts admin).
# Flip SOLO_MODE_ENABLED=false to restore full commercial multi-tenant platform.

SOLO_MODE_ENABLED=true
DEFAULT_TENANT_PLATFORM_MODE=internal
PRODUCTION_SECURITY_MODE=true

# Guest / M2M off
BALLROOM_GUEST_WS=false
HIVE_DASHBOARD_GUEST_WS=false

# Operator revenue — ON (configure Stripe in Settings → Billing)
LEADERBOARD_ENABLED=true
VERIFIED_POLLEN_LEADERBOARD_ENABLED=true
SKILL_MARKETPLACE_UGC_ENABLED=true
BEE_GAMIFICATION_ENABLED=true
UGC_CONTENT_ENGINE_ENABLED=true
RECIPE_MARKETPLACE_BETA_ENABLED=true
MEDIA_AGENCY_IN_A_BOX_ENABLED=true
MICRO_SAAS_FACTORY_ENABLED=true
SKILL_EXPORT_PREMIUM_ENABLED=true
ENTERPRISE_WORKSPACE_ENABLED=false

# Background jobs — keep agent scheduler + stale sweep; drop marketing rolls
HOURLY_YOUTUBE_CRYPTO_ROLL_ENABLED=false
SUPERVISOR_AUDIT_DIGEST_ENABLED=true
TENANT_AUDIT_RETENTION_ENABLED=true
ALERTING_ENABLED=true
AGENT_STALE_SWEEP_ENABLED=true

# Solo command center on
OPERATOR_LOOP_ENABLED=true
OPERATOR_HUB_SETTINGS_ENABLED=true
AGENT_OS_ENABLED=true
TRADING_COCKPIT_ENABLED=true
PUBLISH_PERFORMANCE_ENABLED=true
RESEARCH_BEE_ENABLED=true
PAPER_TRADING_ENABLED=true

# Live lanes stay OFF until operator explicitly enables
SOCIAL_PUBLISH_LIVE_ENABLED=false
PREDICTION_MARKETS_LIVE_TRADING_ENABLED=false
SOCIAL_PUBLISH_TRUSTED_AUTO_ENABLED=false
SUPERVISOR_PATTERN_ROUTER_LLM_ENABLED=false

# Core on — full agent/knowledge stack
RECIPES_ENABLED=true
SIMULATIONS_ENABLED=true
VOICE_ENABLED=true
DREAMING_ENABLED=true
ROUTINES_ENABLED=true
API_KEY_MANAGEMENT_ENABLED=true
CELERY_WORKER_CONCURRENCY=3

# Optional modules — default ON for admin (override in Settings → Platform → Prostredie)
FORAGER_INTELLIGENCE_LOOP_ENABLED=true

# Frontend build-time hints (must rebuild frontend after change)
NEXT_PUBLIC_SOLO_MODE=true
NEXT_PUBLIC_QS_POLL_PROFILE=vps_32gb
NEXT_PUBLIC_LEADERBOARD_ENABLED=true
NEXT_PUBLIC_RECIPES_ENABLED=true
NEXT_PUBLIC_VOICE_ENABLED=true
NEXT_PUBLIC_HIVE_WS_GUEST=false
NEXT_PUBLIC_BALLROOM_GUEST_WS=false

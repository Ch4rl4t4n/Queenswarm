# /triage-digest — Four-lane digest inbox

1. `/agentic-os#lanes` — pending digests.
2. Promote verified digest → Kanban task (sets `verified_distill`).
3. Zombie cleanup: `./scripts/operator-zombie-session-cleanup.sh`
